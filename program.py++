include 'standard'
include 'graphics.py'

create_window()

include 'standard'

class Car{
    define $struct(name, price, brand){
        let this.name = name
        let this.price = price
        let this.brand = brand
    }

    define start(){
        stdout('VROOM!', this.name)
    }

}
