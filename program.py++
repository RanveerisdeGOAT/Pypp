include 'standard'

class Car{
    define $struct(name, price, brand){
        let this.name = name
        let this.price = price
        let this.brand = brand
    }

    define start(){
        stdout('VROOM!')
    }

}

let car = new Car("BMW", 100000, "IX5")

car.start()
